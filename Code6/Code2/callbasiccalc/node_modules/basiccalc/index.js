
const calcObject = {
    add(first=0, second=0){
        return parseInt(first) + parseInt(second);
    },
    subtractCalc(first=0 , second=0){
        const sub = require("./calc");
        return sub(first, second);
    }
    
}
module.exports = calcObject;